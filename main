<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Favorite Sports Teams</title>
    <link rel="stylesheet" href="styles.css"> </head>
<body>
    <header>
        <h1>내가 좋아하는 스포츠 팀들</h1>
    </header>

    <main class="team-selection">
        <a href="doosan.html" class="team-card doosan-theme">
            <img src="doosan_logo.png" alt="두산 베어스 로고">
            <h2>두산 베어스</h2>
        </a>

        <a href="kia.html" class="team-card kia-theme">
            <img src="kia_logo.png" alt="KIA 타이거즈 로고">
            <h2>KIA 타이거즈</h2>
        </a>

        <a href="t1.html" class="team-card t1-theme">
            <img src="t1_logo.png" alt="T1 로고">
            <h2>T1</h2>
        </a>
    </main>

    <footer>
        <p>경기 일자는 항상 체크합니다!</p>
    </footer>
</body>
</html>
body {
    font-family: 'Arial', sans-serif;
    background-color: #f4f4f4; /* 전체 배경색 */
    color: #333;
    text-align: center;
    margin: 0;
    padding: 0;
}

header {
    background-color: #333;
    color: white;
    padding: 20px 0;
}

.team-selection {
    display: flex;
    justify-content: center;
    gap: 40px; /* 카드 사이 간격 */
    padding: 50px 20px;
}

.team-card {
    text-decoration: none; /* 링크 밑줄 제거 */
    color: inherit;
    width: 200px;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s, box-shadow 0.3s;
}

.team-card:hover {
    transform: translateY(-5px); /* 호버 시 약간 위로 */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

.team-card img {
    width: 100px;
    height: 100px;
    object-fit: contain;
    margin-bottom: 10px;
}

/* 🍎 두산 베어스 테마 (네이비/레드/화이트) */
.doosan-theme {
    background-color: #132448; /* 네이비 */
    color: white;
    border: 3px solid #E6002A; /* 레드 포인트 */
}

/* 🐯 KIA 타이거즈 테마 (레드/블랙/화이트) */
.kia-theme {
    background-color: #C02026; /* 레드 */
    color: white;
    border: 3px solid #000000; /* 블랙 포인트 */
}

/* ⚡️ T1 테마 (레드/블랙/화이트 - e스포츠의 날렵함) */
.t1-theme {
    background-color: #E7002C; /* T1 레드 */
    color: white;
    border: 3px solid #000000; /* 블랙 포인트 */
}

footer {
    padding: 15px 0;
    background-color: #333;
    color: white;
    position: fixed;
    bottom: 0;
    width: 100%;
}
/* doosan-detail.css (별도 파일로 가정) */
.doosan-detail-page {
    background-color: #132448; /* 네이비 주 배경 */
    color: white;
}
.doosan-highlight {
    color: #E6002A; /* 레드 포인트 */
    border-left: 5px solid #E6002A;
    padding-left: 10px;
}
/* kia-detail.css (별도 파일로 가정) */
.kia-detail-page {
    background-color: #C02026; /* 레드 주 배경 */
    color: white;
}
.kia-section {
    background-color: rgba(0, 0, 0, 0.4); /* 블랙 반투명 배경으로 가독성 확보 */
    padding: 20px;
    border-radius: 5px;
}
/* t1-detail.css (별도 파일로 가정) */
.t1-detail-page {
    background-color: #000000; /* 블랙 주 배경 */
    color: #f0f0f0;
}
.t1-neon-line {
    border-bottom: 2px solid #E7002C; /* 네온 느낌의 하단 라인 */
    box-shadow: 0 0 5px #E7002C;
}
.t1-stat-box {
    background-color: #1a1a1a;
    border: 1px solid rgba(231, 0, 44, 0.5); /* 반투명 레드 테두리 */
}
